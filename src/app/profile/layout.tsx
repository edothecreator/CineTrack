export const metadata = {
  title: "Profile",
  description: "Your CineTrack profile and binge stats",
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
